/* e.g. code below is for the main page */
block('blockquote').mod('main-page', true)(
	tag()('blockquote'),
	content()(function(){
		return [
            {
                elem: 'text',
                content: this.ctx.text
            },
            {
                elem: 'slogan',
                content: this.ctx.slogan
            }
		]
	})
)